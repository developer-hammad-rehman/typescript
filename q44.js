"use strict";
function car_storefunc(name, modelnumber, color) {
    let cars = {
        name: name,
        modelnumber: modelnumber,
        color: color,
    };
    console.log(cars);
}
car_storefunc("city", 20, "red");
